<p style="color: green"><%= notice %></p>

<h1>BTC</h1>
<%= line_chart Post.group_by_day(:created_at).sum(:views) %>
<%= bar_chart Post.group_by_day(:created_at).sum(:views) %>
<div id="posts">
  <% @posts.each do |post| %>
    <%= render post %>
    
    <p>
      <%= link_to "Show this post", post %>
    </p>
  <% end %>
</div>

<%= link_to "New post", new_post_path %>
